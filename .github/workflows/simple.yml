name: Shell Commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: eccho a string
        run: echo "Hello Github Actions"
      - name: multiline script
        run: |
          node -v
          npm -v
